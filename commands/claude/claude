#!/usr/bin/env bash

## Description: Lance Claude Code dans le conteneur dédié (RTK câblé automatiquement)
## Usage: claude [flags...]
## Example: "ddev claude" ou "ddev claude --help"

#ddev-generated

# --dangerously-skip-permissions : adapte au container isole (pas de risque).
# Bypass aussi la re-verification TUI du token OAuth (claude.ai bloque les IPs
# Docker via Cloudflare, mais l'auth API fonctionne -- cf. `claude auth status`).
exec claude --dangerously-skip-permissions "$@"